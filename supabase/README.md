# Supabase setup

1. Open your Supabase project dashboard.
2. Go to SQL Editor.
3. Copy and run `schema.sql`.
4. Create `.env.local` in the project root using `.env.example` as the template.
5. Restart the local dev server after editing `.env.local`.
